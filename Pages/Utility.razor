@page "/Utility/{Component?}"
@using BlazorWasmPortfolioGhAction.Components.Ascii
@using BlazorWasmPortfolioGhAction.Components.Teams
@using BlazorWasmPortfolioGhAction.Components.Github
@inject IJSRuntime JSRuntime

<div class="page-section utility-page">
<section>
    <div class="container">
        <div class="section-heading">
            <p class="section-label">TOOLS</p>
            <h2>Developer utilities.</h2>
        </div>

        <div class="utility-layout">
            <aside class="utility-sidebar">
                <nav class="utility-nav" aria-label="Tool categories">
                    @foreach (var category in NavigationItems)
                    {
                        <details class="nav-group" open="@(category == NavigationItems[0])">
                            <summary class="nav-group-title">@category.CategoryName</summary>
                            <ul class="nav-group-items">
                                @foreach (var item in category.Items)
                                {
                                    <li>
                                        <NavLink href="@item.Link" class="nav-item" Match="NavLinkMatch.All">@item.Text</NavLink>
                                    </li>
                                }
                            </ul>
                        </details>
                    }
                </nav>
            </aside>

            <div class="utility-content">
                @if (Component != null)
                {
                    @DynamicComponent
                }
                else
                {
                    <div class="utility-empty">
                        <i class="bi bi-tools" aria-hidden="true"></i>
                        <p>Select a utility from the sidebar.</p>
                    </div>
                }
            </div>
        </div>
    </div>
</section>
</div>

@code {
    [Parameter]
    public string? Component { get; set; }

    private RenderFragment DynamicComponent => Component switch
    {
        "base64" => RenderComponent<BlazorWasmPortfolioGhAction.Components.Base64.Base64>(),
        "map" => RenderComponent<BlazorWasmPortfolioGhAction.Components.MapComponent>(),
        "bigbangcounter" => RenderComponent<BigBangCounter>(),
        "mathcalculator" => RenderComponent<MathCalculator>(),
        "browserstorage" => RenderComponent<BrowserStorageWasm>(),
        "webglrender" => RenderComponent<WebGLRender>(),
        "overnightmintemp" => RenderComponent<OvernightMinTemp>(),
        "cidrcalculator" => RenderComponent<CIDRCalculator>(),
        "geminispellchecker" => RenderComponent<GeminiSpellChecker>(),
        "currenttime" => RenderComponent<CurrentTime>(),
        "ascii" => RenderComponent<BlazorWasmPortfolioGhAction.Components.Ascii.Ascii>(),
        "urls" => RenderUrlComponents(),
        "guid" => RenderComponent<BlazorWasmPortfolioGhAction.Components.GuidGenerator.GuidGenerator>(),
        "sqlite" => RenderComponent<Sqlite>(),
        "html" => RenderComponent<BlazorWasmPortfolioGhAction.Components.HTMLEncodeDecode.HTMLEncodeDecode>(),
        "markdown" => RenderComponent<BlazorWasmPortfolioGhAction.Components.MarkdownConverter.MarkdownConverter>(),
        "timerstopwatch" => RenderTimerStopwatchComponents(),
        "colourconverter" => RenderComponent<BlazorWasmPortfolioGhAction.Components.HexToRGB.HexToRGB>(),
        "binary" => RenderComponent<BlazorWasmPortfolioGhAction.Components.Binary.Binary>(),
        "stringconverter" => RenderComponent<BlazorWasmPortfolioGhAction.Components.StringConverter.StringConverter>(),
        "epoch" => RenderComponent<BlazorWasmPortfolioGhAction.Components.EpochConverter.EpochConverter>(),
        "converter" => RenderConverterComponents(),
        "jwt" => RenderComponent<BlazorWasmPortfolioGhAction.Components.JWTDebugger.JWTDebugger>(),
        "luhn" => RenderComponent<BlazorWasmPortfolioGhAction.Components.LuhnChecker.LuhnChecker>(),
        "hcf" => RenderComponent<BlazorWasmPortfolioGhAction.Components.HCF.HCF>(),
        "unicode" => RenderComponent<BlazorWasmPortfolioGhAction.Components.UnicodePicker.UnicodePicker>(),
        "cryptography" => RenderComponent<BlazorWasmPortfolioGhAction.Components.MD5Converter.MD5Converter>(),
        "fluxortest" => RenderFluxorTestComponents(),
        "qrgenerator" => RenderComponent<QRGenerator>(),
        "randomfact" => RenderComponent<RandomFacts>(),
        "jokequotes" => RenderComponent<JokeQuotes>(),
        "svgdiagrameditor" => RenderComponent<SvgDiagramEditor>(),
        "emailcomposer" => RenderComponent<BlazorWasmPortfolioGhAction.Components.EmailComposer.EmailComposer>(),
        "discord" => RenderComponent<BlazorWasmPortfolioGhAction.Components.Discord.DiscordNotifier>(),
        "slack" => RenderComponent<BlazorWasmPortfolioGhAction.Components.Slack.SlackNotifier>(),
        "teams" => RenderComponent<TeamsNotifier>(),
        "githubsearch" => RenderComponent<GitHubSearcher>(),
        "githubinspector" => RenderComponent<GitHubInspector>(),
        "githubtrending" => RenderComponent<GitHubTrending>(),
        "gist" => RenderComponent<GistCreator>(),
        _ => RenderDefaultComponent()
    };

    private RenderFragment RenderComponent<TComponent>() where TComponent : IComponent
    {
        return builder =>
        {
            builder.OpenComponent<TComponent>(0);
            builder.CloseComponent();
        };
    }

    private RenderFragment RenderUrlComponents() => builder =>
    {
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.UrlSplitter.UrlSplitter>(0);
        builder.CloseComponent();
        builder.AddMarkupContent(1, "<br />");
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.UrlEncode.UrlEncode>(2);
        builder.CloseComponent();
    };

    private RenderFragment RenderTimerStopwatchComponents() => builder =>
    {
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.TimerControl.TimerControl>(0);
        builder.AddAttribute(1, "Input", "5m 00s");
        builder.CloseComponent();
        builder.AddMarkupContent(2, "<hr />");
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.Stopwatch.Stopwatch>(3);
        builder.AddAttribute(4, "Input", "0s 00");
        builder.CloseComponent();
    };

    private RenderFragment RenderConverterComponents() => builder =>
    {
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.HextoDec.HextoDec>(0);
        builder.CloseComponent();
        builder.AddMarkupContent(1, "<br />");
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.MemoryConverter.MemoryConverter>(2);
        builder.CloseComponent();
        builder.AddMarkupContent(3, "<br />");
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.TimeConverter.TimeConverter>(4);
        builder.CloseComponent();
    };

    private RenderFragment RenderFluxorTestComponents() => builder =>
    {
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.FluxorExample.FirstComponent>(0);
        builder.CloseComponent();
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.FluxorExample.SecondComponent>(1);
        builder.CloseComponent();
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.FluxorExample.FetchData>(2);
        builder.CloseComponent();
        builder.OpenComponent<BlazorWasmPortfolioGhAction.Components.FluxorExample.Counter>(3);
        builder.CloseComponent();
    };

    private RenderFragment RenderDefaultComponent() => builder =>
    {
        builder.AddMarkupContent(0, "<p>Select a utility from the sidebar.</p>");
    };

    private List<NavCategory> NavigationItems = new()
    {
        new NavCategory
        {
            CategoryName = "General Tools",
            Items = new List<NavItem>
            {
                new NavItem { Text = "Base64", Link = "Utility/base64" },
                new NavItem { Text = "Urls", Link = "Utility/urls" },
                new NavItem { Text = "Guid", Link = "Utility/guid" },
                new NavItem { Text = "HTML", Link = "Utility/html" },
                new NavItem { Text = "Markdown", Link = "Utility/markdown" },
                new NavItem { Text = "Converter", Link = "Utility/converter" },
                new NavItem { Text = "String Converter", Link = "Utility/stringconverter" },
                new NavItem { Text = "Spell Checker (Gemini)", Link = "Utility/geminispellchecker" },
                new NavItem { Text = "Random Fact", Link = "Utility/randomfact" },
                new NavItem { Text = "Joke quotes", Link = "Utility/jokequotes" },
            }
        },
        new NavCategory
        {
            CategoryName = "SQL Tools",
            Items = new List<NavItem>
            {
                new NavItem { Text = "SQLite Integration", Link = "Utility/sqlite" }
            }
        },
        new NavCategory
        {
            CategoryName = "Cryptography Tools",
            Items = new List<NavItem>
            {
                new NavItem { Text = "JWT", Link = "Utility/jwt" },
                new NavItem { Text = "Luhn", Link = "Utility/luhn" },
                new NavItem { Text = "Cryptography", Link = "Utility/cryptography" }
            }
        },
        new NavCategory
        {
            CategoryName = "Time & Calculation Tools",
            Items = new List<NavItem>
            {
                new NavItem { Text = "Math Calculator", Link = "Utility/mathcalculator" },
                new NavItem { Text = "Min Temp Calculator", Link = "Utility/overnightmintemp" },
                new NavItem { Text = "CIDR Calculator", Link = "Utility/cidrcalculator" },
                new NavItem { Text = "Timer / Stopwatch", Link = "Utility/timerstopwatch" },
                new NavItem { Text = "Epoch", Link = "Utility/epoch" },
                new NavItem { Text = "Current Time", Link = "Utility/currenttime" }
            }
        },
        new NavCategory
        {
            CategoryName = "Encoding & Format Tools",
            Items = new List<NavItem>
            {
                new NavItem { Text = "Ascii", Link = "Utility/ascii" },
                new NavItem { Text = "Colour Converter", Link = "Utility/colourconverter" },
                new NavItem { Text = "Binary", Link = "Utility/binary" },
                new NavItem { Text = "Unicode", Link = "Utility/unicode" },
                new NavItem { Text = "Hidden Char Finder", Link = "Utility/hcf" }
            }
        },
        new NavCategory
        {
            CategoryName = "Testing Library Tools",
            Items = new List<NavItem>
            {
                new NavItem { Text = "Fluxor Test", Link = "Utility/fluxortest" },
                new NavItem { Text = "WebGL Canvas", Link = "Utility/webglrender" },
                new NavItem { Text = "Browser Storage Wasm", Link = "Utility/browserstorage" },
                new NavItem { Text = "QR Generator", Link = "Utility/qrgenerator" },
                new NavItem { Text = "Google Maps", Link = "Utility/map" },
                new NavItem { Text = "Big Bang Counter", Link = "Utility/bigbangcounter" },
                new NavItem { Text = "Svg Diagram Editor", Link = "Utility/svgdiagrameditor" },
                new NavItem { Text = "Email Composer", Link = "Utility/emailcomposer" }
            }
        },
        new NavCategory
        {
            CategoryName = "Payment Tools",
            Items = new List<NavItem>
            {
                new NavItem { Text = "Luhn Checker", Link = "Utility/luhn" }
            }
        },
        new NavCategory
        {
            CategoryName = "DevOps Tools",
            Items = new List<NavItem>
            {
                new NavItem { Text = "Discord Notifier", Link = "Utility/discord" },
                new NavItem { Text = "Slack Notifier", Link = "Utility/slack" },
                new NavItem { Text = "Teams Notifier", Link = "Utility/teams" },
                new NavItem { Text = "GitHub Search", Link = "Utility/githubsearch" },
                new NavItem { Text = "GitHub Inspector", Link = "Utility/githubinspector" },
                new NavItem { Text = "GitHub Trending", Link = "Utility/githubtrending" },
                new NavItem { Text = "Gist Creator", Link = "Utility/gist" }
            }
        }
    };

    public class NavCategory
    {
        public string CategoryName { get; set; } = "";
        public List<NavItem> Items { get; set; } = new();
    }

    public class NavItem
    {
        public string Text { get; set; } = "";
        public string Link { get; set; } = "";
    }
}